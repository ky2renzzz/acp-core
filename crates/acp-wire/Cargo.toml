[package]
name = "acp-wire"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "JSON-RPC 2.0 wire format for the Agent Client Protocol (ACP): newline-delimited framing, message classification, canonical JSON (RFC 8785 JCS), blake3 hashing."
readme = "README.md"
keywords = ["acp", "json-rpc", "canonical-json", "jcs", "agent"]
categories = ["encoding", "parsing", "development-tools"]

[dependencies]
serde       = { workspace = true }
serde_json  = { workspace = true }
blake3      = { workspace = true }
thiserror   = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
